import Vue from 'vue'
import Vuex from 'vuex'
import ldp_store from './modules/ldp_store'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: { ldp_store
  }
})
